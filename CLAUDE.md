# NSE Trading Dashboard — Project Memory

## Project
**NSE 4-Strategy Dashboard** — single-file `nse_live_dashboard.html` (~2,600 lines, no framework), Python scripts in `nse-trading-bot/`, GitHub Actions cron, Vercel static + serverless. Repo: `Dushyant-dataanalyst/job-command-center`. Owner: Dushyant, ₹1Cr goal in 2 years, ~₹1.5L MIS capital.

## Key facts
- **4-Strategy** = 4 voters (Inna, Pham, Cianni, Unger) each vote BUY/STRONG_BUY/WATCH/NO_SIGNAL per stock; weighted consensus via `voter_weights.json`. Logic in `equity_scan_core.py` is a *reconstruction* from one-line hints, not a verified spec.
- Every JSON feed at repo root is CI-generated; dashboard fetches client-side. `vercel.json` needs a no-cache route per new JSON file.
- Timestamps: always use `ist_time.now_ist_str()` — bare `datetime.now()` on CI runners is UTC mislabeled as IST (past bug).
- All JSON writes: `write_text(..., encoding="utf-8")` — Windows cp1252 default corrupted files before.
- Emoji in Python source/print crashes Windows cp1252 console — use `[OK]`/`[WARNING]` in scripts.
- **Repo is PUBLIC as of 06-Jul-2026 (was private) — this is deliberate, planned to REVERT TO PRIVATE ~01-Aug-2026.** Cause: hit 100% of the 2,000 free Actions minutes/month days into the cycle (private-repo quota resets 01-Aug-2026) — the external cron-job.org trigger fired `workflow_dispatch` every 5 min 24/7 unrestricted, and GitHub bills every job invocation rounded UP to a full minute, so even the `gate` job's fast skip decision off-hours cost a full billed minute, ~180×/day on top of market-hours runs. Rather than throttle further, went public — public repos get free/unlimited Actions minutes on standard runners, which fully removes the billing constraint. **Accepted tradeoff while public: `kite_session.json` is committed daily by `kite_auth_refresh.yml` and holds a live Kite `access_token` — anyone can read it and call Kite read-endpoints (holdings/positions/margins/trades/orders) with the user's real account until the repo reverts to private.** A reminder is scheduled for 01-Aug-2026 (also see project memory) — if this project comes up in a session at or after that date and the repo is still public, flag it proactively; the reason to stay public no longer applies once the original quota has reset.
- `nse_fo_refresh.yml` — back to the original two-job `gate`→`fo-refresh` structure with the full `schedule:` (5-min market hours, hourly off-hours, once-daily 9pm prep) + `workflow_dispatch`, restored 06-Jul-2026 once the repo went public. The off-hours throttle (`nse-trading-bot/should_run_full_refresh.py`, 55-min gap check) is kept as a **product decision** now, not cost control — 5-min granularity off-hours has no signal value (market's closed) and only adds commit/Vercel-redeploy noise. Heartbeat (healthchecks.io) lives in `gate` so it fires on every trigger even when `fo-refresh` is skipped. **If the repo ever reverts to private while cron-job.org's unrestricted external trigger is still active, this WILL recreate the original billing crisis** — before that revert (or before relying on this design past 01-Aug-2026), move the run/skip decision off GitHub Actions entirely (e.g. a Vercel serverless function gating before it ever calls `workflow_dispatch`) so a "decide to skip" never costs a billed Actions minute again.
- `concurrency.cancel-in-progress` is `false` (not `true`) — `logs/run_history.json` changes every execution and forces a commit, so commit-count is a reliable execution-count proxy; with `true`, most runs got killed by the next 5-min tick before reaching the commit step. Don't re-flip this without re-checking commit cadence first.
- GitHub's native `schedule:` trigger is documented best-effort and can silently skip ticks — known issue, don't re-diagnose from scratch. Mitigated (not fixed) by the external cron-job.org trigger hitting `workflow_dispatch` independently (now harmless cost-wise while the repo is public).
- Kite Connect (`kite_fallback.py`): daily manual token via `kite_auth_refresh.yml` workflow_dispatch; session in `kite_session.json` (expires daily). Two roles: (a) quote-price *fallback* when yfinance fails; (b) real portfolio reads — `get_holdings/get_positions/get_margins/get_trades/get_orders`. `kite_portfolio_refresh.py` writes `kite_portfolio.json` (real holdings/margin/reconciliation vs manual tracking, plus appends real fills to `kite_trade_history.json` — Kite's API is day-scoped only, no historical trade retrieval, so this is the sole way to accumulate real trade history) each run, failing closed+graceful (`session_live:false`) when token stale; dashboard "Real Portfolio" panel in My Positions (My Positions tab de-dupes against it via `KITE_HELD_SYMBOLS` — a stock shown there with real Kite data doesn't also get a conflicting manual card). **No orders** (SEBI static IP). `kite_portfolio.json`/`kite_trade_history.json` are *public* static feeds (user accepted holdings exposure). **Phase 2 done and enabled**: `market_data.get_ohlcv()` tries Kite historical first (`USE_KITE_HISTORICAL=true` in the workflow env) when a live session + instrument-token resolve, falls back to yfinance automatically otherwise — wired into `refresh_fo_cloud.py`/`equity_scan_core.py`/`market_regime_core.py`/`sector_rotation_core.py`. Verified live: candles match yfinance exactly on close/volume; fixed a real premium-formula bug in the process (`_premium_estimate` was scoring favorable ITM moves as losses — no intrinsic-value floor).
- **Telegram policy (user request 06-Jul-2026): NO routine per-run pings.** `signal_alerts.py` (end of each refresh) batches ONLY new actionable events into one message — F&O consensus flipping to BUY_CE/BUY_PE, new equity STRONG_BUYs, paper-trade closes, real tracked positions crossing SL/T1/T2, and **`_kite_fo_watch_events`: real held Kite F&O positions where the underlying signal flips AGAINST the held direction** (hold a CE, consensus goes WAIT/BUY_PE = exit-review ping; exact SL/T1 alerts aren't possible server-side since the user's F&O premium targets live only in browser localStorage, never synced). All deduped across runs via `logs/alert_state.json` (committed CI state; deliberately NOT a dashboard feed, so not in vercel.json/SCHEMA). NOTE: Kite `/positions` occasionally returns an empty `net` post-market even with a live session (seen 06-Jul-2026 16:02, back by 16:26) — the F&O watch skips its state-prune when positions[] is empty so a blip doesn't forget a still-open position, and the dashboard's F&O section just hides for that one refresh. `record_run_outcome.py` alerts CRITICAL on step failures only — its INFO-success-every-run ping was removed; "pipeline went silent" is healthchecks.io's job. Don't re-add success pings.
- **Daily report (`daily_report.py` → Telegram, built 07-Jul-2026, user asked for it)**: ONE progress digest per day (Trading Brain win/loss/expectancy first, then recommendation-journal + strategy-performance progress, then a **NEXT SESSION OUTLOOK** — user asked for "next day prediction + market strategy", built as an HONEST rule-based outlook, explicitly NOT a fabricated price forecast: per-index regime+ADX, ATR-based typical 1-day range, expert-gate posture, `market_regime.json`'s own best-fit/avoid strategy recommendation, mood + top sectors, and a rule-based plan line synthesized from gate+regime). Fired ONLY on the once-daily 9pm IST prep run (`30 15 * * 1-5`) + workflow_dispatch — NOT the 5-min cadence, so it does NOT violate the no-per-run-pings rule (it's a deliberate daily opt-in). Shows day-over-day DELTAS via `logs/daily_report_state.json` (CI state, not a dashboard feed — in git-add only, not SCHEMA/vercel). Idempotent per day (dedups on that state's date), and a failed Telegram send does NOT mark the day done, so it retries rather than silently skipping. INFO level. This is the ONLY sanctioned routine daily Telegram message — still no per-refresh pings.
- **Twin implementations — the premium model exists in TWO places that must stay identical**: `refresh_fo_cloud.py::_premium_estimate()` (Python) and `nse_live_dashboard.html::_estimatePremium()` (JS, My F&O Positions panel). Guarded by `nse-trading-bot/check_premium_parity.py` + `.github/workflows/premium_parity.yml` (fires on push when either file changes; extracts the real JS, runs it in Node, compares 122 cases, asserts premium ≥ intrinsic in both). Changing one side without the other = CI failure, by design — this exact drift misprized a real position 04–06 Jul 2026 (ITM winner shown as loser). Known smaller twins not yet guarded: `_daysToExpiry` (JS `Math.ceil`) vs Python's `.days` floor — ±1 day premium wobble, cosmetic; the workflow's inline `pip install` pins vs root `requirements.txt`.
- New JSON feed checklist (3 places, all or it silently breaks): workflow `git add` list + `validate_json_outputs.py` SCHEMA + `vercel.json` (builds AND no-cache route — the `/(.*)`  catch-all otherwise serves the dashboard HTML instead of the JSON). `kite_trade_history.json` was missing from vercel.json until 06-Jul-2026.
- **Data-source provenance was silently lying (fixed 07-Jul-2026)**: `market_data.get_ohlcv()` has correctly returned `(df, "kite"|"yfinance")` since Phase 2, and `_get_indicators()`-style helpers in all signal cores captured it into `v["data_source"]` — but the final output assembly in `refresh_fo_cloud.py`, `equity_scan_core.py`/`equity_scan_refresh.py`, `sector_rotation_core.py`, and `stock_fo_refresh.py` (this last one found and fixed in a second pass, during an unrelated audit-document compilation — same bug, initially missed) either dropped the field entirely or hardcoded a literal `"yfinance EOD"` string regardless of what was actually used. Verified live: Kite historical was actually succeeding for NIFTY50/BANKNIFTY, all 49 equity-scan stocks, and all 18 stock-F&O stocks the whole time — every "yfinance EOD" label shown all session was false for those. Fixed by threading `data_source` through to the real output and computing an honest aggregate label (`kite` / `yfinance` / `mixed: N kite / M yfinance`) instead of a hardcoded string, in all 4 scripts. `market_regime_core.py` was already doing this correctly — worth checking as the reference pattern if this drifts again. Also surfaced: the 10 sector *indices* (`^CNXIT`, `^CNXAUTO`, etc.) aren't in `kite_instruments.INDEX_TOKENS` (only NIFTY/BANKNIFTY/VIX are), so sector-level fetches in `sector_rotation_core.py` still fall back to yfinance even though the individual stocks within those sectors correctly resolve via Kite's equity dump — not fixed, just documented; would need the actual Kite instrument_tokens for each sector index before adding them.
- **Recommendation journal (`recommendation_tracker.py` → `recommendation_journal.json`, built 07-Jul-2026)** = item 1+2 of the "learning engine" roadmap: scores the engine's OWN signals (index F&O / single-leg + spread stock F&O / equity BUY+STRONG_BUY) whether or not the user acted, as virtual recs with status open/won/lost/expired/invalidated. Runs every refresh (not once-daily gated) after the signal files it reads. NOT a duplicate of `trade_brain.py` (still the richer F&O paper-trade sim) or `voter_weights_refresh.py` (still learns per-voter weights from real closed trades) — it's the broad forward-looking observer over ALL signal types; `backtest.py` is the historical counterpart. Honest limits baked in + labeled: F&O marks use the estimated-premium model (`entry_basis="premium_estimate"`), stock-F&O spreads scored only coarsely vs breakeven at expiry (`spread_underlying_vs_breakeven`, no two-leg time-value modeling), equity re-marks only when `equity_scan.json` refreshes daily, and the spec's "false-flip" metric is a documented TODO (needs post-invalidation tracking), not half-built. Required `stock_fo_refresh.py` to start surfacing `ann_vol` in its output so single-leg stock F&O recs can be re-priced like the index. Roadmap not-yet-built (in build order): `expert_gate.py` (state machine to cut CE/PE flip noise — CHANGES live signal behavior, needs care), `strategy_performance.py` (full learning report — overlaps existing `_compute_stats`/`voter_weights`/`backtest`, dedupe before building), dashboard win/loss panel, real-F&O structured trade journal (still the open gap from the audit). User directive: NO rejected-order alerts, keep repo public for now, NO real auto-trading yet — "expert observer → expert paper trader → only much later executor."
- **Expert gate (`expert_gate.py` → `expert_gate.json` + `logs/expert_gate_state.json`, built 07-Jul-2026)** = roadmap item 3/5, a confirmation+cooldown STATE MACHINE over the raw NIFTY/BANKNIFTY index-F&O signal to kill CE/PE whipsaw. States WATCH→SETUP_FORMING→CONFIRMED_ENTRY→IN_TRADE→EXIT_WATCH→EXIT_CONFIRMED→COOLDOWN. Tunable constants at top (`ENTRY_PERSIST_REFRESHES=2`, `EXIT_PERSIST_REFRESHES=2`, `COOLDOWN_REFRESHES=3`, `MIN_ADX_FOR_ENTRY=20`, `MIN_VOTES_FOR_ENTRY=4` — raise to 5 to "prefer 5/6+ setups"). Entry blocked in choppy/low-ADX regime + first/last 15 min (reuses `config.py` window) + off-hours; reversal CE↔PE needs MORE votes than the held side had, else just EXIT_WATCH. `advance()` is pure/unit-testable (like `should_run_full_refresh.decide()`). **It does NOT place/size/authorize any trade — no executor exists (SEBI static-IP block); it only decides which raw signals are "confirmed setups."** **Behavior change wired 07-Jul-2026**: `signal_alerts._fo_buy_events` now fires F&O ENTRY alerts on the gate's `entry_confirmed_this_run`, NOT raw consensus flips — but **FAILS OPEN** (gate feed missing/broken → reverts to raw-flip alerts, labeled `[raw signal -- expert gate unavailable]`; a gate bug can make alerts noisier, never silent). Real-held-position EXIT alerts (`_kite_fo_watch_events`) are deliberately NOT gated — those protect live capital, don't delay them behind persistence.
- **Strategy performance (`strategy_performance.py` → `strategy_performance.json`, built 07-Jul-2026)** = roadmap item 4, the unified win/expectancy/profit-factor/maxDD scoreboard over `recommendation_journal.json`, sliced by kind/vote-count/direction/regime/voter/time-of-day. READ-ONLY, no behavior change. Deliberately does NOT replace `trade_brain._compute_stats` (F&O paper) / `voter_weights_refresh` (real closed equity) / `backtest.py` (historical) — it's the cross-cutting forward scoreboard those three don't cover. Sharpe/maxDD method matches `backtest.py` for comparability (same disclosed compounding caveat). "false-flip" true metric still deferred; reports a labeled PROXY (`invalidated_while_in_profit`). Shows a sample-size warning under 20 decisive recs.
- Learning-engine roadmap status: items 1 (journal) + 2 (tracker) + 3 (expert gate) + 4 (perf report) built & wired. NOT built: adaptive scoring that consumes the perf report to auto-down-rank negative-expectancy setups (item 5 — needs enough real closed recs first), dashboard panels for the 3 new feeds (`recommendation_journal`/`expert_gate`/`strategy_performance` are fetchable but not yet rendered), and the real-F&O structured trade journal (still the audit's open gap). CI step order (all every-refresh unless noted): ...equity_scan(daily)→snapshot(daily)→**expert_gate→recommendation_tracker→strategy_performance**→signal_alerts→validate→commit.
- **Macro risk overlay (`macro_risk_refresh.py` → `macro_risk.json`, built 08-Jul-2026)** — user request after being "disappointed" the bot didn't catch a real market flip; this is the "what's happening outside the charts" module the technical engine has zero visibility into. It is a RISK OVERLAY, not a signal generator — never emits BUY/SELL itself, only `risk_score`(0-100)/`risk_level`(LOW/MODERATE/HIGH/EXTREME)/`bias`/`confidence` + `trade_adjustments` (position-size multiplier, min-votes/confirmation-refresh escalation, avoid/watch sectors, blocked tickers) meant for a **future** `expert_gate.py` wiring to blend with technical_signal+market_regime+recommendation_history — that wiring is explicitly NOT done (build-order item 6 of 6; this covered items 1-5: script, JSON, `vercel.json` route, workflow step, dashboard panel). **Deterministic only, no LLM interpretation of headlines anywhere** — two factor kinds: (1) market-data factors (crude Brent `BZ=F`, USD/INR `INR=X`, India VIX, US VIX, S&P/Nasdaq/Dow composite, gold) tiered on %-move vs previous close via named-constant thresholds in source (`MARKET-DATA TIERS`); (2) news factors from two real sources only — GDELT DOC 2.0 API (free, no key) for geopolitical/war keywords, and `news_feed.json`'s own `position_news` (NSE's official RSS, already fetched earlier in the same CI run by `news_refresh.py`) scanned for fraud/scam/SEBI/probe keywords against held tickers. Anti-hallucination rules actually enforced in code, not just docs: no source URL+timestamp → factor is dropped, not guessed; news >48h old excluded outright; a geopolitical factor backed by only 1 distinct source domain is capped `evidence_class:"rumor"` (severity 2/12 pts) and mathematically cannot alone reach the HIGH-risk point range — needs ≥3 corroborating domains for the top severity/20-30pt tier. Unavailable inputs (FII/DII flows — no free non-scraped source found; Gift Nifty — no stable free source verified; RBI/Fed calendar — deferred) are listed under `data_unavailable`, never faked as zero-risk. **Workflow placement**: step runs after BOTH `market_mood_refresh` and `news_refresh` (not immediately after market_mood as the brief literally said) so its regulatory-alert factor reads this run's fresh `news_feed.json`, not a stale one — `STEP_MACRO_RISK` wired into `record_run_outcome.py` (auto-picked-up via env-var prefix scan, no code change needed there) and `macro_risk.json` added to the git-add list + `vercel.json` build/no-cache-route + `validate_json_outputs.py` SCHEMA. Dashboard panel renders on the Overview tab (`ov-macro-risk`, after the Market Regime panel) — verified in the browser preview against both real fetched data and a synthetic EXTREME-risk payload (multi-factor, rumor tag, stock alert, both avoid+watch sector chips) to confirm no layout breakage before restoring the real file. **Wired into live trade gating same day** (closing build-order item 6, via a new shared `macro_gate.py` helper — `load_macro_risk()`/`direction_blocked()`/`escalated()`/`macro_context()`, all fail-open on a missing/broken feed): `expert_gate.py`'s `advance()` now BLOCKS `CONFIRMED_ENTRY` outright when macro's `allow_new_longs`/`allow_new_shorts` disallows that direction, and ESCALATES (never loosens) `min_votes_required`/`confirmation_refreshes` — verified with a synthetic EXTREME-bearish payload: `BUY_CE` stayed permanently blocked, `BUY_PE` needed 4 persistent refreshes instead of 2, matching the original brief's own worked examples exactly. `trade_brain.py` (F&O paper-trade opens) gates new entries the same way and stamps `macro_context` on every trade. `stock_fo_refresh.py` (18-stock advisory signals, no auto-open exists) FLAGS rather than suppresses — a human decides manually there, and hiding the raw technical signal would violate the overlay's own "doesn't replace the technical signal" disclaimer. Equity has no automated trade-opening code at all (manual "I bought" only), so nothing to gate there.
- **GDELT reliability finding + RSS fallback (08-Jul-2026, same day as the macro overlay build)**: the "not yet CI-verified" GDELT caveat above turned out real — confirmed via a live GitHub Actions run (not just local network): first hit GDELT's own documented `HTTP 429` ("limit requests to one every 5 seconds", tripped by manual testing bursts + a retry with NO backoff re-violating it instantly), then on the next run a genuine `ConnectTimeout` reaching `api.gdeltproject.org` from GitHub's hosted runner itself — a real infra reliability issue with GDELT, confirmed independently from two different networks (this dev machine's ISP AND GitHub Actions), not a local block. Fixed two ways: (1) `_fetch_gdelt_articles()` retry now sleeps 8s before retrying (was 0s) and `geopolitical_feed_status` now carries the real HTTP status/exception detail instead of a bare `"fetch_failed"`, diagnosable straight from the committed JSON without needing Actions log access (this session had no `gh` CLI available); (2) added `_fetch_rss_geopolitical_articles()` as a same-run fallback — BBC World + Al Jazeera RSS (`RSS_GEOPOLITICAL_FEEDS`, no key/auth, same proven plain-XML technique `news_refresh.py` already uses for NSE's own RSS), tried only when GDELT fails outright. Every geopolitical factor now records `news_source` (`"gdelt"` or `"rss_fallback"`) for transparency. **Also caught and fixed a real false-positive bug while verifying the fallback**: keyword matching was naive substring (`kw in text`), which flagged an unrelated US Senate-race headline as "geopolitical escalation" (matched `"war"` inside some unrelated word, not the standalone word) — replaced with `_keyword_hit()`, a shared regex word-boundary matcher (`\bkeyword\b`, case-insensitive) used by BOTH the geopolitical RSS scan and `_regulatory_alerts()`'s fraud/scam/SEBI scan (which had the identical class of bug, just not yet caught in practice). Re-verified after the fix: the RSS fallback correctly surfaced a genuine, current, market-relevant story ("Oil prices surge as US strikes Iran, reversing slide to pre-war levels") as a single-source `rumor`-class factor — exactly the capped-severity behavior the anti-hallucination design intends. Known accepted residual limitation (documented in `_keyword_hit()`'s docstring, not silently ignored): a few keywords are still contextually ambiguous even as whole words (e.g. "invasion" in "invasion of privacy") — deliberately not fixed with NLP/LLM semantic understanding since that would violate the module's own "deterministic only, no LLM interpretation" design; the corroborating-source-count requirement is the intended backstop against one stray ambiguous match driving risk up alone.
- **Macro overlay wired into live trade gating (08-Jul-2026)** — new shared `macro_gate.py` (`load_macro_risk()`/`direction_blocked()`/`escalated()`/`macro_context()`, all fail-open on a missing/broken feed). `expert_gate.py`'s `advance()` now BLOCKS `CONFIRMED_ENTRY` outright when macro's `allow_new_longs`/`allow_new_shorts` disallows that direction, and ESCALATES (never loosens) `min_votes_required`/`confirmation_refreshes` — verified with a synthetic EXTREME-bearish payload matching the original brief's own worked examples exactly. `trade_brain.py` (F&O paper-trade opens) gates new entries the same way and stamps `macro_context` on every trade. `stock_fo_refresh.py` (18-stock advisory signals, no auto-open exists) FLAGS rather than suppresses — a human decides manually there.
- **Stock F&O was never wired into Telegram alerts (found + fixed 08-Jul-2026)** — user reported "why am I not seeing any put calls." Root cause: `signal_alerts.py` only ever read `fo_latest.json` (index F&O) and `equity_scan.json` (equity STRONG_BUY) — `stock_fo.json` (18-stock F&O, BOTH CE and PE) was never read at all, in either direction, since the file was created. Not a CE/PE bias. Fixed with `_stock_fo_buy_events()`, same raw-consensus-change dedup pattern as `_fo_buy_events`'s fail-open branch (stock F&O has no expert_gate coverage — that gate's SCOPE is explicitly index-only). Verified locally: correctly emits both CE and PE stock-F&O alerts, dedupes on repeat runs, confirmed live in production CI.
- **Trade commentary (08-Jul-2026)** — user asked to see WHY each suggested trade fires, not just the raw signal. Added purely-additive commentary functions (never touch the actual vote logic `backtest.py`/`backtest_fo.py` import directly, zero drift risk preserved): `_signal_commentary()` in `refresh_fo_cloud.py` explains `_signals()`'s 4-factor F&O vote (EMA stack/MACD/pivot breakout/ROC5) with real numbers, shared by index F&O and stock F&O since both call the same `_signals()`. `_voter_commentary()` in `equity_scan_core.py` explains each of Inna/Pham/Cianni/Unger's verdict, mirroring their exact conditions so the text can't drift from the real vote (spot-checked against ~10 real stocks across both BUY and WATCH states, matched every time). Dashboard: visible commentary line + enriched vote-badge tooltips on the equity BUY table, a commentary box on F&O index cards, hover tooltip (💬 icon) on the stock F&O signal badge. Verified live in production.
- **Real, evidenced finding while investigating "why did we miss [a rival channel's NIFTY PE call]" (08-Jul-2026)**: pulled today's actual `fo_latest.json` commit history — NIFTY fell ~24398→~23982 (~1.7%) over the session, a genuine tradeable intraday decline, but our PE vote count stayed frozen at 2/6 (never reaching the 4-vote threshold) for 4+ hours while spot drifted 100+ points, because 2 of the 4 F&O factors are structurally too slow for single-day moves: MACD (lagging) stayed bullish the whole session, and the EMA18/EMA50 stack never cleanly aligned bearish since EMA50 is a 50-*day* average. Only the pivot-breakdown factor (correctly) flagged bearish. **Conclusion: the 4-factor F&O consensus is a swing-timeframe model, not built to react within a single trending day** — a real, legitimate gap, unrelated to astrology. NOT yet fixed — `MIN_VOTES_FOR_ENTRY`/the factor set are not arbitrary (backtest_fo.py showed real edge: 55.5% win rate, Sharpe 4.18 for index F&O at current thresholds), so loosening them to catch more days like this needs a proper re-backtest first, not an eyeballed tweak. Flagged for the user, not yet actioned.
- **Astrological Market View (`astro_market_view.py` → `astro_view.json`, built 08-Jul-2026)** — added at the user's EXPLICIT request, after being shown (in the same conversation) a rival Telegram channel's own astrology-flavored call failing and getting mocked by its subscribers, and after Claude raised that exact concern via `AskUserQuestion`. User chose "add it anyway, clearly labeled" over 3 other options (skip it / track rival channels' real performance instead / just wanted the analysis) — proceed only within those chosen guardrails, do not silently drop the isolation constraints if asked to "simplify" this later. Two strictly separated layers: (1) `astronomical_facts` — REAL, verifiable, deterministic data via `pyephem` (moon phase/illumination, Mercury retrograde status computed from actual day-over-day geocentric ecliptic longitude, NOT a hardcoded date table — hardcoding specific dates without a live source to verify against would itself have been an unverified claim); (2) `folk_market_view` — explicitly labeled traditional/folk belief text, with the disclaimer noting academic literature (Yuan/Zheng/Zhu 2006) finds at most a marginal, contested lunar-cycle effect on stocks. **ISOLATION is the actual point of this module, not a footnote**: `astro_view.json` is read by NOTHING in the trade-decision path — not `macro_risk_refresh.py`, `macro_gate.py`, `expert_gate.py`, `trade_brain.py`, `stock_fo_refresh.py`, or `equity_scan_core.py` — verified by grepping the whole repo (only `validate_json_outputs.py`'s schema check references it). Contributes zero points to any risk_score, blocks nothing, sizes nothing. Gated to the once-daily prep run only (moon phase/Mercury status don't meaningfully change within a trading day — no value in the 5-min cadence). Dashboard panel (`ov-astro`) deliberately styled DIFFERENT from every trading panel — dashed purple border, no green/red signal colors, no vote badges, no buy button, "UNVERIFIED, NOT A SIGNAL" in the header, full disclaimer always visible (not tooltip-hidden). Added `ephem==4.2.1` to the workflow's pip install line — first new Python dependency added to this pipeline since the original build.
- `daily-jobs.yml` + `fetch_jobs.py` = unrelated job-search feature in same repo; don't touch.

## Preferences (standing)
- **Never handle credentials** — user adds tokens via GitHub/Vercel UI only; if pasted in chat, tell them to revoke.
- Verify with real data before claiming done; no fabricated numbers; label estimates honestly.
- Signals are educational; SEBI: manual orders only; never enable live trading autonomously.
- Rebase over CI bot commits (`git pull --rebase`, take `--theirs` for data JSONs) before push.
- Light theme on this dashboard (dark premium preferred elsewhere — ask first).
- Responsive pattern: `clamp()` type, `auto-fill` minmax grids, scroll-snap tabs, column-hiding tables.

## Master Brief (Downloads/NSE_Trading_System_Master_Brief.md)
Locked config: risk 1% MIS / 1.5% CNC, min R:R 1.5/2.0, max 20%/position, kill switches 2% daily / 5% weekly, DRY_RUN default — all in `nse-trading-bot/config.py`. These 2%/5% kill switches are Module 3 (not built), and are intentionally distinct from the dashboard's own existing circuit breaker (`DAILY_MAX_LOSS_PCT`/`WEEKLY_MAX_LOSS_PCT` = -3%/-8% in `nse_live_dashboard.html`) — user explicitly said keep the dashboard one as-is, don't reconcile the two. Order code needs static whitelisted IP (SEBI Apr 2026) — cannot run on GH Actions/Vercel. Build gates: audit → approval → backtest FIRST → executor → filters → safety. System B (quality-growth screen) = separate folder, never orders.
- **Module 2 (`nse-trading-bot/trade_filters.py`) built, filters 1-4 only**, per brief's own "build 1-4, pause, then 5-6": market regime (reads `market_regime.json`'s avoid-list), time window (first/last 15min + optional lunch), loss streak breaker (3 consecutive losses), R:R rejection (1.5 MIS / 2.0 CNC). Standalone/testable via `python trade_filters.py` — **not yet wired into any trade-opening code** (e.g. `trade_brain.py`'s `_open_trade()`) since that would change existing paper-trading behavior; needs explicit user go-ahead first. Filters 5 (correlation/exposure) and 6 (liquidity/spread) not built — paused per the brief.
- **Gate 2 (`nse-trading-bot/backtest.py`) built 07-Jul-2026** — replays the live 4-voter equity logic (imports `_strategy_inna/_pham/_cianni/_unger` directly from `equity_scan_core.py`, zero drift risk vs. what's actually live) against 3 years of real yfinance daily data across the 46-stock universe. Standalone, read-only, on-demand (`python backtest.py`, not wired into any CI cron, no dashboard feed, deliberately not in vercel.json/SCHEMA). Disclosed simplifications baked into every output: flat equal voter weights throughout (no retroactive replay of `voter_weights_refresh.py`'s learning), PEG untested (no historical source, Pham tested technical-half-only), an invented exit rule since none exists live for equities (SL-or-T1-or-20-trading-day-timeout — verified only 5.7% of trades actually hit the timeout, so this isn't diluting the result), trade-return-based Sharpe (not NAV-based), and a chronologically-compounded max-drawdown that overstates real risk (doesn't model spreading capital across concurrent positions). **First real run's result (49 stocks, 3y, 1,486 trades) is a genuine "not yet a clear go" signal, not a rubber stamp**: overall 49.1% win rate / Sharpe~2.05 / max DD -54.14%, three of four voters (Inna 49.0%, Cianni 45.2%, Unger 48.7%) are statistically indistinguishable from a coin flip and **Cianni is net-negative (-0.06% avg)**, only Pham shows real edge (55.8% win rate, best avg return) — and the whole system's per-trade returns look weak next to a plain 12.26%/yr buy-and-hold of the same universe. Don't reflexively re-run this expecting a different verdict without changing the strategy logic or the universe — the data currently argues Pham should be weighted up and Cianni questioned, well before `voter_weights_refresh.py` would ever accumulate enough real trades to learn the same thing itself.
- **F&O Gate 2 (`nse-trading-bot/backtest_fo.py`) built 08-Jul-2026** — the equity backtest's counterpart, covering what it doesn't: index F&O (NIFTY50/BANKNIFTY 3-factor consensus, `refresh_fo_cloud.py`) + stock F&O (18 tracked stocks, single-leg AND spread, `stock_fo_refresh.py`). Same rigor: `_signals`/`_premium_estimate`/`_atm` imported directly (zero drift on the decision/pricing math); the only backtest-specific code is the walk-forward indicator series and `_next_monthly_expiry_asof()` (a date-parameterized twin of `_next_monthly_expiry()`'s calendar math, since that function hardcodes `date.today()` — verified byte-identical output against the real function). Cross-checked the walk-forward indicators against production `_get_indicators()` — matched exactly when fed identical data (a same-day live-fetch timing artifact caused a *false* mismatch on first check, resolved by re-testing on a shared dataframe, not a logic bug). **Unlike equity, F&O has a REAL live exit rule already** (`trade_brain.py`'s target2>target1>stop_loss>expired>signal_flip priority) — reused as-is, no invented convention needed for single-leg trades. Spreads settle only at expiry vs breakeven (same coarse convention as `recommendation_tracker.py`'s `fo_stock_spread` scoring — no two-leg time-value modeling). Sharpe/max-drawdown use the identical method as `backtest.py` for direct comparability.
  **First real run (3y, 1,616 trades total)** — genuinely mixed, worth reading carefully, not averaging away: **Index F&O shows real edge** (218 trades, 55.5% win rate, Sharpe 4.18 — NIFTY50 alone 58.2%/Sharpe 3.44, BANKNIFTY 52.8%/Sharpe 2.47 — meaningfully better than equity's near-coin-flip result). **Stock F&O single-leg (max-conviction 6/6 votes) is sub-coin-flip** (804 trades, 39.2% win rate). **Stock spreads (4-5 votes) also sub-coin-flip on win rate** (594 trades, 38.6%) **but show a large positive average return (116.07%)** — a real, structural property of capped-risk debit spreads (small net-debit base means an uncapped-until-width win dwarfs a capped -100% loss in % terms), not an error; read win-rate AND Sharpe/maxDD together for these, never avg_return_pct alone. **Counter-intuitive finding worth acting on**: by vote-count, 6/6 "max conviction" stock trades had the WORST win rate (39.8%) and lowest avg return (11.0%) of any bucket — WORSE than the 4-5 vote spread bucket (41.9%/45.3% win, 92.84%/110.85% avg) — directly contradicting the "prefer higher vote counts" intuition baked into `expert_gate.py`'s `MIN_VOTES_FOR_ENTRY` tunable comment. Don't raise that threshold expecting it to help stock F&O without accounting for this. Max-drawdown reads ~-100% almost everywhere — expected given the compounding-overstates-risk caveat (options frequently do lose 100% of premium; chronologically compounding 800+ trades makes hitting that trough at least once nearly inevitable) — this is NOT "you'd lose all your capital," see the disclosed method caveat before reacting to that number alone.
