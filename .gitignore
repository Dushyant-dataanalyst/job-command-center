# ── PRIVATE — never commit ──────────────────────────────
.env
*.key
*.pem
*.p12
secrets.txt

# CV / personal documents — keep off GitHub
*.pdf
*.docx
*.doc

# Claude Code local config & memory — personal data inside
.claude/

# Local server & utility scripts — not needed on Vercel
start-dashboard.bat
stop-dashboard.bat
refresh-jobs.bat
screenshot.py

# Python artifacts
*.log
__pycache__/
*.pyc
.DS_Store
